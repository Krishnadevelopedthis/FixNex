# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.1

# ---- Data / ORM ----
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.7.4
pydantic-core==2.18.4
pydantic-settings==2.5.0
email-validator==2.2.0

# ---- Security ----
argon2-cffi==23.1.0
PyJWT==2.10.1
cvss==3.4

# ---- Background processing ----
celery==5.3.6
redis==5.0.1

# ---- Object storage (evidence) ----
minio==7.2.0

# ---- Scanner / enrichment HTTP client ----
httpx==0.27.0

# ---- AI triage (optional) ----
# Only used when ANTHROPIC_API_KEY is set; the feature reports itself as
# unavailable and the platform is unaffected when it is not.
anthropic>=1.3,<2

# ---- Reporting ----
Jinja2==3.1.5
fpdf2==2.8.2
pandas==2.1.4
openpyxl==3.1.1

# ---- Testing ----
pytest==8.0.0
pytest-cov==5.0.0
